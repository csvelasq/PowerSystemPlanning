<UserControl x:Class="PowerSystemPlanningWpfApp.Analysis.TransmissionExpansionPlanDetailsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PowerSystemPlanningWpfApp.Analysis"
             xmlns:localUtils="clr-namespace:PowerSystemPlanningWpfApp.ControlUtils"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="280" Background="White">
    <DockPanel LastChildFill="True">
        <StackPanel DockPanel.Dock="Top">
            <!--Number of built transmission lines-->
            <TextBlock>
                <TextBlock.Text>
                    <MultiBinding StringFormat="{}{0} transmission lines built in this expansion plan">
                        <Binding Path="NumberOfBuiltTransmissionLines" />
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
            <!--Total investment cost-->
            <StackPanel Orientation="Horizontal">
                <Label Content="Total investment cost (MUS$):" />
                <TextBox Width="100" Height="20" Text="{Binding TotalInvestmentCost, StringFormat=C}" />
            </StackPanel>
            <!--Show only selected?-->
            <StackPanel Orientation="Horizontal">
                <CheckBox />
                <TextBlock Text="Show only selected transmission lines?" Margin="5,0,0,0" />
            </StackPanel>
        </StackPanel>
        <!--Details of the current expansion plan-->
        <localUtils:CustomDataGrid ItemsSource="{Binding CandidateTransmissionLinesToBuild}" AutoGenerateColumns="False">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="ContentTemplate">
                        <Setter.Value>
                            <DataTemplate>
                                <TextBlock TextWrapping="Wrap" Text="{Binding}"></TextBlock>
                            </DataTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Is Built?" Binding="{Binding IsBuilt}" IsThreeState="False" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="100" />
                <DataGridTextColumn Header="Investment cost (MUS$)" Binding="{Binding InvestmentCost, StringFormat=C}" Width="80" />
            </DataGrid.Columns>
        </localUtils:CustomDataGrid>
    </DockPanel>
</UserControl>
